/** Único arquivo de cores a editar para trocar a paleta: `colorTemplate.ts` */
export {
  colorTemplate,
  buildGlowFromPrimary,
  hexToRgb,
  type ColorTemplate,
} from './colorTemplate'
export { fonts, lp, glow } from './tokens'
export { createAppTheme } from './createAppTheme'
